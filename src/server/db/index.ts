/**
 * 🎯 CONNEXION DATABASE COMPATIBLE BUILD
 * Import depuis le module de connexion optimisé
 */

// Import de la connexion standard (compatible build)
export { db } from "./connection";

// Export de la fonction async pour les cas avancés
export { getDatabaseAsync } from "./connection";
